# Copiar a .env y ajustar. El .env real NO se versiona.

PORT=3000
HOST=0.0.0.0

# Ruta del archivo SQLite. En producción apunta al volumen montado (ej. /data/sgo.sqlite).
SGO_DB_PATH=./data/sgo.sqlite

# fatal | error | warn | info | debug | trace
LOG_LEVEL=info

NODE_ENV=development
